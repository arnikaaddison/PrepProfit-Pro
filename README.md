# PrepProfit Pro

A single-file dashboard for tax preparers to track commission splits, referral payouts, and client return earnings.

Open `index.html` in a browser — no build step or server required. Data is stored locally in the browser (localStorage).
